# 工业控件与持久化实施状态

## 表格内导出交付

- 用户最新要求仅完成表格内导出，其余待办不做；覆盖此前自动持续推进约定。
- 数据表新增下载图标和 CSV/PSREC 菜单、取消按钮与独立导出状态。
  历史表冻结点击时查询条件和卷快照；实时表冻结有界缓存内全部匹配行，均忽略分页。
- 复用保存对话框授权、后台导出、文件大小限额、CRC/类型化 CSV 和原子替换。
  新增有界实时记录导出队列，最多 1000 行、合计受 queue_bytes 约束，不写入历史或录制计数。
- 选择文件期间翻页、更新数据或刷新历史不改变导出内容；禁用状态在选择结果返回时重验，
  旧运行时代次拒绝；内部任务/对话框不泄漏给 Lua 业务回调。
- 专项 3/3，3.50 秒；全量构建和 CTest 35/35，50.14 秒；
  无 GUI 表格/导出专项 2/2，2.80 秒。GUI 实际点击经 Application/worker 导出并读取 CSV 验证。
- OpenGL 1000px/360px 非空和布局检查通过，两张表格截图已人工查看。
  未新增长期测试或其余扩展，未将尚未运行的 24 小时验收标为通过。

## 数据表选中行 API

- 新增 `proto.ui.get_selected_row(id)`，复用类型化记录转换，返回当前页选中行的独立副本；
  ID 保留为十进制字符串，不经浮点转换，读取不执行磁盘 I/O。
- 待加载脚本不能读取旧 runtime；无选中、加载/错误页、换页或实时淘汰后返回 nil。
  单行数据不写入 ControlValue，也不持有全历史。
- 表格专项覆盖历史/实时回调、返回副本修改隔离、未知 ID、淘汰与重载；
  Manifest、LuaLS 和文档已同步。专项及 LuaLS 2/2，全量 CTest 35/35，43.46 秒。
- 表格内导出操作与完整长期故障注入仍需继续，自动推进约定不变。

## 持续记录与容量余量

对应提交：`238de02`。

- 新增独立 `protoscope_storage_soak_benchmark`，每秒 1000 条，256KiB 卷、8MiB 总量，
  同时核对滚动清理、当前保留行、并发查询/PSREC 导出及内存采样，不加入普通 60 秒 CTest。
- 持续测试先复现容量边界与短查询占用重叠时的故障；清理提前到硬上限的 90%，
  无法达到低水位本身不报故障，实际超过硬上限仍明确失败。确定性回归覆盖未知文件保护。
- 60 秒结果：提交 60000，清理 42800，保留 17200，清理 77 卷，
  查询/导出各 2400，失败 0，预热后工作集峰值增长 1040384 字节。
- 300 秒结果：提交 300000，清理 284500，保留 15500，清理 509 卷，
  查询/导出各 12000，失败 0，预热后工作集峰值增长 3862528 字节。
- 运行命令与验收口径见 `storage-soak-test.md`；24 小时测试仍未运行。
- 全量回归曾出现原始采集诊断异步回写断言失败；总单测随后连续三次通过，
  尚不能据此声称消除了时序风险。该模块未做无关修改。
- 最新全量 CTest 35/35，43.46 秒；无 GUI 存储/表格/协调器专项 3/3，7.21 秒。

## 自动轮转与旧库迁移接线

对应提交：`d5269f7`。

- `RecordVolumeCoordinator` 已加入构建并接入 Store 写线程，相邻发布命令仍合并为一次事务；
  非空卷按 UTC 日或 `max_volume_bytes`（默认 256MiB，含 WAL）在批次前轮转。
- 旧 `records.sqlite` 经身份校验、检查点和持久化迁移意图后封存登记；
  四个迁移中断阶段可重试，未知目标不覆盖，记录 ID、int64 和未停止意图保留。
- 轮转先保存目标和边界，再准备新卷、封存登记旧卷、提交活动指针；
  已提交记录先计入 committed，后续位置索引失败不能误算为写入失败。
- 热重载暂停释放活动连接，恢复按持久化活动指针重建；共享活动引用保护旧快照，
  切换中间态不会重复查询旧源。新增测试先复现再修复 resume 沿用旧录制开关的问题。
- 协调器专项覆盖容量/跨日、重复时间戳、三个待切换阶段、位置索引落后、
  模式变化、未停止任务新卷恢复及 Store 替换交接；迁移专项覆盖 Store 启动接线。
- 本轮全量构建通过，CTest 35/35，48.40 秒；
  无 GUI 存储/Lua/迁移/协调器专项 4/4，13.94 秒；差异空白检查通过。
- 尚未验收所有半初始化准备目录的恢复；损坏或身份无法验证时明确报错，不删除未知数据。
  持续负载、完整磁盘故障注入和 24 小时稳定性仍未完成，自动继续后续验收及表格接口。

## 会话与切换意图持久化

- 独立 `RecordSession` 将版本化状态写入目录索引，保存录制意图、会话/运行编号、
  异常退出次数、正常退出标记、最后提交位置、故障范围和活动/待切换卷身份。
- 已接入 Store 的意图、位置和退出生命周期；失败重载恢复旧线程时重新读取会话状态。
  暂停后销毁旧 Store 不再覆盖新 Store 的运行状态。记录已提交但位置索引失败时仍保留正确提交计数，
  同时进入可见故障，重启以记录数据库实际提交位置校验索引。
- 已提交中断范围跨重启恢复；Lua 状态新增 session_id/run_id/unclean_recovery/abnormal_runs。
  软件子进程直接退出（跳过析构）验证 WAL 恢复、异常标记和主动停止不自动重启。
- 活动卷身份可先保留到切换意图，再创建固定路径文件；未提交的替换不会改变原活动身份。
- 全量构建及 CTest 33/33，43.96 秒；无 GUI 存储/Lua/会话专项 3/3，10.37 秒。
- 仍未接入 Store 自动轮转和旧库迁移协调器；遇到待恢复的活动指针会明确拒绝加载，
  不会误建空活动库覆盖该状态。继续实现完整迁移/切换恢复，再进行持续性能验收。

## 查询源切换与单写者交接

对应提交：`1386e0e`。

- `RecordQueryService::switchActive` 在快照锁内登记旧卷并替换活动源，
  旧快照持有固定路径和活动卷引用；新快照不重复计算已封存旧卷。
- 同一进程内同根 Store 也执行单写者约束，原生查询对象/结果可独立延长目录占用。
- 首次全量回归发现与原热重载“先激活新 Store”顺序冲突；
  加载器改为声明验证后排空并暂停旧 Store，再激活新 Store，激活失败恢复旧线程及回调。
  暂停保留已完成任务队列和缓存，不丢弃失败重载后的旧 KV 回调。
- 修复后全量构建及 CTest 32/32，42.58 秒；无 GUI 存储/Lua/活动卷专项 3/3，8.95 秒。
  包含新 KV 配额导致激活失败后旧会话继续执行写队列的回归。
- 仍待持久化活动指针/切换意图、旧活动库迁移与 Store 自动轮转接线。

## 固定路径活动卷基础

对应提交：`721e6e7`。

- 独立 `RecordVolume` 负责创建/重开、整批校验提交、已提交计数和 ID 范围、UTC 跨日/容量判断、
  固定路径封存；默认轮转容量判断为 256MiB，空卷不反复轮转。
- 封存使用 PASSIVE checkpoint，保持 WAL 格式并释放写连接，不改名已有查询的源文件。
  已验证旧 SQLite 读事务仍看到旧快照，新读者读取全部提交数据，封存不等待旧读事务结束。
- `VolumeCatalog::adoptRecording` 校验封存卷、登记模式及稳定 ID 范围，并接管活动快照占用引用。
  记录卷支持 WAL/SHM 身份边界和精确文件清理；跨卷查询高水位改为实际最大行 ID，
  不再误用记录条数。
- 全量构建及 CTest 32/32，38.66 秒；无 GUI 活动卷专项通过，0.26 秒。
  覆盖行 ID 1000/1001、INT64_MAX、批次原子拒绝、所有权/元数据损坏、重开和占用引用交接。
- 仍为内部基础，Store 尚未切换到此模块。必须继续实现活动卷指针与切换事务、
  既有 records.sqlite 迁移、旧快照来源切换，以及跨重启会话/异常退出标记。
  新建但尚未登记的活动卷目前不会自动接管或删除，恢复策略仍待完成。

## 周期清理与容量故障接线

对应提交：`a0261e9`。

- Store 写线程周期维护，启动记录和登记导入前强制检查；
  未占用快照缓存可回收，UI 当前页和正在导出的引用仍保护卷。
- 超限且无可清理卷或清理 I/O 失败时，记录转入故障；后续记录明确失败并累计中断范围，
  KV 与历史查询保持可用。最新已提交 ID/时间在重启时从活动库恢复。
- `scripting.storage` 已接通队列、批次、提交周期、最大记录字节、保留天数、维护周期、
  KV 值/总量/深度配置，应用装配到 worker 和 Lua 数据会话；重载协议生效。
- Lua status/任务事件新增最后提交位置和当前运行期中断时间范围。
  中断标记尚未跨重启持久化；目录采样不是硬磁盘配额，允许监测周期内短时超量。
- 全量构建及 CTest 31/31，42.60 秒；无 GUI 存储/配置/Lua 专项 3/3，8.02 秒。
- 自动按日/256MiB轮转仍待实现，活动库自身超限目前会明确故障，不会删除活动库。
  后续轮转必须保持旧快照源路径稳定，不能直接改名仍被查询占用的 SQLite 文件。

## 清理安全层

对应提交：`228696b`。

- 内部 `VolumeCatalog::retain` 支持按封存时间和记录目录实际文件字节清理，
  默认 30 天/10GiB；容量包含活动库、WAL、目录索引及暂存文件，不删除它们。
- 仅删除已索引、自有身份复验通过且未占用的封存卷；不递归删除、不跟随链接，
  超额且没有可清理卷时明确返回 `capacityExceeded`。
- 清理意图先以 FULL 同步写入索引，文件移除后才删除索引映射；
  重启恢复“尚未删除”和“数据库已删、标记仍在”的窗口，未知文件/身份不匹配拒绝清理。
- 同进程目录索引共享互斥与卷占用表，快照延长目录锁生命期。
  记录根使用进程独占文件锁，Store 在打开活动库前取得锁。
- 全量构建及 CTest 31/31，43.68 秒；无 GUI 目录专项通过，0.75 秒。
  Windows 已验证跨进程拒绝和占用文件删除失败后的重启恢复；非 Windows 分支尚未实机验证。
- 尚未接入 Store 周期监测、自动轮转及容量故障状态，不应称为长期记录验收完成。

## 异步导入闭环

对应提交：`1ec6da3`。

- `Store::importRecords` 与 `proto.record.import` 已接入有界后台任务队列，
  支持 PSREC、工具 CSV 和引用已声明数据集的普通 CSV 映射。
- 完整暂存校验后登记目录索引，取消检查覆盖暂存及索引提交前；
  不触发 publish、latest、设备命令或实时计数。成功后新查询才纳入导入卷。
- Lua 使用文件读取授权和 `max_file_size_bytes`；保存对话框的仅写授权不可借用。
  完成事件报告源路径和登记记录数；失败释放任务配额，不暴露部分记录。
- 示例新增导入/取消菜单，Manifest、LuaLS 和 API 文档同步。
- 全量构建通过，CTest 31/31，44.40 秒；无 GUI 导入/Lua 专项 2/2，9.16 秒。
  覆盖 1500 行、重复时间戳、固定快照、重启、取消、CRC、容量、读取授权及实时状态隔离。
- 本阶段没有完成全局容量清理、自动轮转或异常退出新卷恢复，继续下一阶段。

## 跨卷查询接线

对应提交：`6cad3f7`。

- Store 已接入目录索引及独立 RecordQueryService；查询和导出共用固定卷集合的快照。
- 每卷保留一个排序键，通过堆合并后读取命中 payload；模式版本映射到目录全局版本，
  重复时间戳按跨卷稳定行 ID 排序，不把整段历史复制到 UI。
- 快照最多缓存 128 个，未占用者可淘汰；历史表持有引用保护翻页用卷。
  元数据预算为约 32MiB 的保留量估算，不代表精确 RSS 限额。
- 初步专项：cross_volume_query/storage/record_export/data_table 4/4，2.91 秒。
- 首次全量回归发现旧整数帮助函数截断 Lua 快照令牌；数据 API 改为直接读取 Lua int64，
  同时验证 INT64_MAX 设备时间和拒绝小数查询参数。修复后全量构建及 CTest 31/31，
  41.67 秒；无 GUI 跨卷/Lua 数据专项 2/2，7.08 秒。
- 自动轮转、清理和异步导入尚未完成，按计划继续执行。

## 分卷目录索引基础

对应提交：`4b2a3a3`。以下为该阶段完成时的边界，Store 接线见上节。

- 内部 `VolumeCatalog` 建立独立 `records/index.sqlite`，仅登记校验成功的自有封存卷；
  同根移动完成后提交索引，索引失败时仍由暂存对象拥有文件。
- 验证协议、版本、数据库身份、卷身份、计数和所有权标记，拒绝未知文件与链接。
  模式登记提供卷内版本到目录全局版本映射；导入记录分配不重用的高位 ID 区间。
- 引用式卷占用保护已具备；索引和卷重启校验、相同本地版本的不同模式隔离等专项两组通过。
  全量构建通过，CTest 30/30，42.69 秒。
- 尚未接入 Store 的跨卷查询、自动分卷或清理；当前只是内部索引模块，不应宣称 Lua 导入闭环完成。
  下一步替换单库读取服务，给每个快照固定各卷高水位并持有占用引用，再接入异步导入。

## 导入暂存基础

对应提交：`89189bf`。

- 内部 `stageRecordImport` 逐条解析 PSREC、工具 CSV 或声明映射的普通 CSV，
  使用独立 SQLite/WAL 暂存卷与批量事务；完整结束校验通过才标记 sealed。
- 暂存未登记前，现有查询不可见，不更新 latest、记录计数或业务回调。
  保留源协议、设备、时间、模式版本和字段类型。共用内部 SQLite RAII 实现。
- 监测源文件读取字节以及暂存数据库/WAL/SHM 占用；失败/取消按所有权标记清理，
  不递归删除、不沿暂存目录链接越界，身份不匹配时保留文件供显式恢复检查。
- 两组专项通过，覆盖三种格式、1500 行往返、重复时间戳、超过首个提交批次后的取消、
  损坏尾部、容量超限和身份不匹配保护；全量 CTest 29/29，37.64 秒，无 GUI 专项通过。
- 仅为内部隔离暂存基础，尚未提供 `proto.record.import`。
  下一步将暂存卷接入目录索引及跨卷查询，成功登记前不向用户报告导入完成。

## 异步记录导出

对应提交：`8a8e186`。

- `Store::exportRecords` 和 `proto.record.export` 复用固定快照、字段筛选、全历史排序；
  独立文件模块逐行写入 PSREC/CSV，不复制全历史，成功后才原子替换目标。
- Lua 路径沿用既有文件授权与写入限额，默认不覆盖；存储层拒绝 records/KV 目录，
  取消、写失败、超限和目标已存在都保留原文件。导出计数与实时记录计数独立。
- 示例增加系统保存对话框、导出/取消菜单，改为连续采样，移除采满 100 条自动停止。
- 导出专项两组通过，覆盖 2000 行中的 1500 行固定快照筛选排序、两种格式、
  中文路径、取消、文件限额、覆盖保护和保留目录；Lua 数据专项现为 9 组。
- 全量构建通过，CTest 28/28，39.67 秒；无 GUI 导出与 Lua 专项 2/2。
- 后续继续暂存导入、目录索引、分卷与完整恢复。表格自身导出按钮及选中行内容 API 尚未接入。

## CSV 编解码与共享输出

对应提交：`1e9b65a`。

- 类型化 CSV 保留模式、类型标签、显式 null 和结束计数；普通 CSV 按已声明字段映射，
  拒绝非法引号、重复表头、缺列、溢出和非有限值；不隐式填补旧值。
- 复用既有 CSV 转义规则，将临时文件替换实现提取到数据层，旧 plot API 保持兼容。
- 五组 CSV 专项通过，包含 BOM 后带引号表头（先复现后修复）、取消保护原文件和临时文件清理。
  完整构建通过，CTest 27/27，39.12 秒；无 GUI PSREC/CSV 专项 2/2。
- 交换格式详见 `record-csv-format.md`。后续导出进展见上节；暂存导入和表格按钮仍待完成。

## PSREC 编解码基础

对应提交：`12e62da`。

- 新增独立 `data::PsrecWriter/PsrecReader`，块式处理且不依赖 SQLite、Lua 或 GUI。
  版本头、类型化模式/记录、块序号、CRC32 和结束计数详见 `psrec-format.md`。
- 四组专项通过：精确位型往返、逐字节损坏、全部截断前缀、输出失败/非法状态、
  取消及解码节点放大限制。完整构建通过；全量 CTest 26/26，35.85 秒。
- 本阶段只验证编解码；后续导出与原子输出进展见上节。
  暂存导入和记录目录登记仍待实现。

## 数据表阶段验证

对应提交：`18893b9`。下一工作线是文件交换编解码和异步任务接入。

- 独立 `data::LiveTable` 保留有界记录；`DataTableSession` 管理当前页、固定快照和查询替换。
  UI 快照共享不可变页，不将历史放进控件值；历史查询返回稳定 `record_id`。
- 实时/历史表支持列配置、单列排序、类型化筛选、选择、分页、刷新和查询取消。
  worker 校验运行时代次、显隐、禁用、只读和历史页版本。
- 新增宿主回归覆盖滚动、查询替换、迟到结果、重载、刷新回首页及取消保留快照；
  最后两项均先复现失败再修正。内部查询不触发业务 `on_record`。
- 全量构建通过，`ctest --test-dir build --output-on-failure --timeout 60`：
  25/25，通过耗时 37.03 秒；无 GUI 表格/存储专项此前 2/2 通过。
- 真实 ImGui → Application → worker 回归覆盖选择、筛选、实时和历史翻页、刷新。
  1000px/360px OpenGL 帧非空，几何无溢出；已查看图标字体加载后的 360px 截图。
- 示例新增 Tables 页签，Manifest/LuaLS 与文档同步。
- 尚未接入表格导出、选中行内容 API、长期分卷和文件交换；继续自动推进这些项。

本文件记录已实现边界，不替代完整扩展方案。当前提供基础 Lua 记录示例，
已接入基础工业控件及有界实时/历史表，尚未提供文件交换的完整界面。

## 已验证的基础修复与执行保护

- 定时器在同一轮被取消或重新设置后，不再执行旧代次回调。
- 文件发送通过请求 ID 跟踪在途块；请求块等待 `completed`，普通发送等待 `sent`。
  失败、拒绝、丢弃、取消及超时终止文件任务并释放句柄。
- 追加写入按整个文件计入限额，多追加句柄共享限制；关闭显式报告刷新及关闭错误。
- 重载校验控件类型及选项兼容性，实测表值和发送运行状态不跨重载保留。
- Lua 指令预算覆盖加载声明和业务回调，默认加载 5s、回调 500ms；
  worker 停止信号可以中断 Lua 忙循环，超时后成功重载可恢复。

对应提交：`128dec2`。文件 API 参数和预算说明见 `lua-host-integration.md`。

## 数据与存储基础

### 模块边界

- `protoscope_data`：纯 C++20 类型化值、模式、记录、验证及二进制值编码。
  不依赖 Lua、ImGui 或 SQLite。
- `protoscope_storage`：基于固定 SQLite 3.50.4 的独立 C++ 服务。
  已通过独立 ScriptDataSession 接入 Lua worker 和应用配置。
- `protoscope_sqlite`：随仓库静态编译，关闭动态扩展加载；来源和归档摘要见第三方目录说明。

### 当前 C++ 接口

`storage::Store(root, protocol, schemas, config)` 接收已解析的协议标识、专用根目录及固定模式。
根目录由调用方传入，应用默认使用可执行目录下的 data，支持 `scripting.storage.root_dir`。

- `start/stop/set/erase/flush/query` 返回任务 ID；`poll()` 返回完成结果。
- `publish` 整批校验后进入有界记录队列，成功代表已入队。
- `status` 分开报告收到、入队、提交和失败数；队列满或事务失败进入记录故障。
- `get` 只解码已提交的内存缓存，不执行磁盘 I/O；异步写入成功提交后才更新缓存。
- 析构等待已接受的写入处理完毕；查询使用独立只读连接和后台线程。
- `cancel` 设置查询取消标记，完成事件可能为已成功结果或取消错误，不返回半页数据。

默认记录队列 32MiB，批处理目标 100ms / 1000 行；单次 `publish` 批次保持在同一事务中，
因此该批次大于目标行数时事务也可能超过 1000 行。写连接使用 WAL 和 `synchronous=FULL`。
KV 单值 256KiB、深度 16、每协议 8MiB。最多保留 1024 个未消费异步任务，
其中查询最多 16 个，并对查询页及模式元数据施加结果内存预算。

### 当前磁盘格式

```text
<调用方指定的协议根目录>/
  kv/values.sqlite
  records/records.sqlite
```

两类数据库均验证应用身份、协议归属、格式版本和完整性。
模式变化登记新版本，查询页携带该页涉及的历史模式，不重解释旧记录。
查询按接收时间、记录 ID 排序，使用已提交 ID 高水位固定快照；
默认每页 200 条，上限 1000 条，支持设备、数据集和时间条件。

记录启动状态持久化，未主动停止时重新打开 Store 恢复开关。
这只是恢复基础，尚未实现“重启开启新分卷”和异常退出区间标记。
KV 独立于记录文件，记录故障不阻断 KV 任务。

## Lua 接入

- Lua 基础发布、KV、记录启停/状态/查询/取消及回调已接入；详见 `lua-data-storage.md`。
  加载阶段禁止运行期 API，KV 初始化读取在成功激活后提供。
- 每个 runtime 独占会话，旧任务不跨重载投递；KV 缓存只在提交后更新。
- 转换检查循环、混合键、稀疏数组、大小和深度，保留 int64、null、字节和嵌入 NUL。
- 存储轮询使用固定到期时间，连续收包或获取快照不会延迟任务结果。
- `protocols/data_storage_demo/main.lua` 使用工业控件和状态栏展示基础发布、记录和查询。

## 剩余工作

- 现有控件的属性原子更新、worker 显隐/禁用/只读及约束校验已接入；
  见 `lua-control-properties.md`。
- 现有 UI 控件、示波器切换、弹窗/文件对话框已加入运行时代次隔离。
- 8 种基础工业控件、编辑草稿、新输入 UI 记忆、tabs、业务菜单和 Lua Dock 显隐已接入；
  data_table 和实测字段绑定已接入，表格导出仍待接入。
- 实时字段绑定、字段条件查询、全历史排序、有界实时数据表及历史页已接入。
- 记录分卷、目录索引、占用保护、滚动清理、磁盘容量监测和完整重启恢复。
- CSV 与 `.psrec` 导入导出、暂存分卷与导入取消。
- 已提供基础存储示例及对应 Manifest/LuaLS/文档；完整 UI 与导入导出示例仍待完成。
- 故障注入、持续 1000 条/秒以及 24 小时稳定性验收。

## 验证

```powershell
cmake -S . -B build
cmake --build build -j 4
ctest --test-dir build --output-on-failure
```

已有 `build` 缓存使用 MinGW Makefiles，不能直接用 `-G Ninja` 改写该缓存。
新增安全与存储测试独立于 GUI，CTest 超时均为 60s。
目前未执行磁盘满故障注入和 24 小时稳定性测试。

本轮 Lua 接入验证：

- `cmake --build build -j 4`：通过。
- `ctest --test-dir build --output-on-failure`：21/21 通过，33.91 秒。
- 新增 `protoscope_script_data_tests`：记录分页、KV 重载、输入验证、声明预算、worker 回调。
- 专项测试曾连续 5 次通过；补充固定轮询期限断言后全量测试通过。
- `build-industrial-headless` 的 Lua 数据目标构建及专项测试通过。
- `build/tests/protoscope_storage_rate_benchmark.exe 10`：
  `rate=1000/s seconds=10 received=10000 queued=10000 committed=10000 queried=10000 failed=0`。
  工具接受秒数参数（1..86400），目前仅运行 10 秒；不包含清理、导出和内存增长验收。

动态属性基础验证：

- `protoscope_control_properties_tests`：3 组通过，覆盖整批回滚、worker 拒绝非法事件和重载约束。
- `cmake --build build -j 4`：通过，包含 GUI；首次重编译超过命令超时，确认进程退出后增量重跑成功。
- `ctest --test-dir build --output-on-failure`：22/22 通过，37.05 秒。
- GUI 显隐、禁用和只读呈现尚未进行人工交互验收。

运行时代次验证：属性专项现为 5 组，新增旧输入拒绝、失败重载保留代次和
旧文件对话框不授予新协议路径权限。完整构建通过；CTest 22/22，34.02 秒。

基础工业控件验证：

- `label/readout/indicator/progress/slider_int/slider_float/radio_group/text_area` 已接入。
  新控件声明和属性见 `lua-control-properties.md`，Manifest/LuaLS 已同步。
- 实测输出不持久化；新输入加入既有 UI YAML。readout 返回格式化字符串，
  int64 不经 double；更新时间和存储轮询均使用宿主 system_clock 毫秒时基。
- 草稿与宿主分离，支持 change/commit、失焦和释放提交、Escape 取消；
  worker 再验约束，代次变化、禁用、只读或停止绘制会取消草稿。
- `protoscope_control_properties_tests` 7 组通过；GUI 新增真实 ImGui 输入帧，
  覆盖滑块释放、宿主更新保护、多行普通 Enter、失焦提交和 Escape 取消。
  Escape 测试先复现错误提交，修正后通过。
- `cmake --build build -j 4` 通过；CTest 23/23，33.27 秒。
- `build/tests/protoscope_industrial_ui_tests.exe protocols/data_storage_demo <临时截图目录>`
  通过 1000px/360px 几何、非空 OpenGL 帧、输入持久化检查，已查看截图。
  合成输入测试验证 GUI 提交行为，不替代真实设备端到端与长期稳定性验收。

基础工业控件提交：`126a9b0`。

固定 tabs 验证：

- 独立 `tabs_layout` 模块解析固定页签，通过稳定页 ID 复用控件 worker 事件与 YAML 记忆。
- 宿主专项现为 8 组，覆盖默认页、非法/重复 ID、只读/禁用基础链路、旧代次、
  重载保留、页序重排、删除所选页后回退默认页与非法记忆拒绝。
- GUI 真实输入帧覆盖程序切页不回传与用户点击回传，使用 ImGui 页签矩形定位点击。
  已查看 360px tabs OpenGL 截图，1000px/360px 非空检查通过。
- 全量构建通过，CTest 23/23，35.55 秒；Manifest/LuaLS 同步检查通过。

固定页签提交：`7d0b46d`。

业务菜单验证：

- 独立业务菜单状态与解析模块，`set_menu/update_menu/show_dock/on_menu` 已接入，
  新业务菜单入口与内置菜单隔离；Lua Dock 显隐沿用原有状态文件。
- 菜单可以在待加载 runtime 中声明，失败重载不污染旧菜单；运行时代次与菜单树版本
  拒绝旧事件，worker 检查祖先和当前项的显隐、禁用；主线程只排队，不等待回调。
- `protoscope_business_ui_tests` 4 组通过，包含循环/稀疏/超限、整批与单项原子校验、
  重载隔离、worker 分派及 Lua 回调预算超时后重载恢复。
- 实际 ImGui → Application → worker → Lua → 快照测试通过：
  菜单点击隐藏 Dock，手动显示后旧请求不再覆盖用户操作。
- 全量构建通过；CTest 24/24，33.83 秒；Manifest/LuaLS 同步检查通过。

业务菜单提交：`076a7e2`。

字段绑定验证：

- 独立绑定模块检查固定数据集/字段类型，支持按设备过滤的
  label/readout/indicator/progress；发布时逐记录更新，不复制历史。
- 显示状态明确区分等待、有效、显式空值和转换失败；无效时 `get_control` 返回 nil，
  不将旧读数当成当前值。数据拥有的值不接受脚本 set_control 或 value 补丁覆盖。
- 新增 int64 精度、设备过滤、非法整批不更新、重载清空、空值、转换错误不影响提交、
  存储错误不阻断实时绑定的回归。数据专项现为 7 组，通过。
- 全量构建通过，CTest 24/24，31.84 秒；OpenGL GUI 专项重跑通过。
- 无 GUI 目录重新配置后，数据、菜单和属性三个目标构建及专项 3/3 通过，5.77 秒。
  首次多目标构建因旧 Makefile 未识别新增菜单目标而失败，重新配置后已复验。

字段绑定提交：`b1dea11`。

历史表查询前置：

- `protoscope_data` 增加无 Lua/SQLite 依赖的类型化条件及比较逻辑。
- SQLite 读线程在分页前应用字段条件及字段排序，仍使用固定高水位；
  字段名和值参数化，固定操作符，不开放任意 SQL，不更改已有数据库结构。
- C++ 存储专项现为 9 组，Lua 数据专项 8 组，覆盖 int64 极值、字节/显式空值、
  类型隔离、重复排序键分页、新记录不进入旧快照和字段名 SQL 字符。
- 完整构建通过，CTest 24/24，35.59 秒；Manifest/LuaLS 检查通过。
  尚未进行大容量字段排序性能验收，排序可能使用 SQLite 临时文件。
